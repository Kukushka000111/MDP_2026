# Client (Planned)

Frontend implementation (React + Tailwind + Leaflet) will be added in the next stage.

Important UX requirement already accounted for:

- favorites for unauthenticated users are supported via `localStorage`
